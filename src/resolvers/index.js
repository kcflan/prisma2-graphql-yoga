const { Query } = require("./query");
const { Mutation } = require("./mutation");
// const { Subscription } = require("./subscription.js");

module.exports = {
  Query,
  Mutation,
  //   Subscription,
  //   Add others, ie. post, user, lead
};
